name: check-versions

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  versions-in-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Catalog and plugin.json versions match
        run: bash scripts/check-versions.sh
